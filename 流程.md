**本教程**将指导如何配置 `nftables` 防火墙，将特定的 HTTP 端口流量通过 `TPROXY` 方式透明拦截，并交给 `hev-socks5-proxy` 处理，最终由 `hev-socks5-proxy` 将流量转发给 `ua3f` 进行后续处理。

## 前提条件

### **准备工作：安装所需软件**

0.认证插件[多用户多接口支持的重庆大学校园网认证客户端.](https://github.com/lurenjiamax/luci-app-cquauth/tree/main)

1. 安装 [hev-socks5-proxy](https://github.com/heiher/hev-socks5-tproxy)：

   ```bash
   opkg update
   opkg install hev-socks5-proxy
   #手动下载安装 scp D:\exp\hev-socks5-tproxy-linux-mips32elsf root@192.168.1.1:/usr/bin/hev-socks5-tproxy
   ```
   
2. 确认 [ua3f](https://blog.sunbk201.site/posts/ua3f/) 已安装并运行:

   确保您之前安装的 ua3f 插件工作正常。

   **安装**UA3F：

   ``` bash
   opkg update
   opkg install curl libcurl luci-compat
   export url='https://blog.sunbk201.site/cdn' && sh -c "$(curl -kfsSl $url/install.sh)"
   ```

   **开启UA3F:**

   在`Openwrt`打开该服务，*或者，手动启动该服务，可以在官方Git找到相应的命令*

   确保enable,user-agent填为`FFF`（可选）或者`自定义UA`
   
   **设置防火墙规则**
   
   1.`Nftables`参考如下配置文件将其加入`/etc/nfts/100-tproxy.nft` ,此处的`nft-version`满足如下条件
   
   ``` bash
   root@OpenWrt:~# opkg list-installed | grep nftables
   nftables-json - 1.1.1-r1
   root@OpenWrt:~# nft --version
   nftables v1.1.1 (Commodore Bullmoose #2)
   ```
   
   `/etc/nfts/100-tproxy.nft`
   
   ```bash
   #!/usr/sbin/nft -f
   
   table inet tproxy_table {
       chain prerouting {
           type filter hook prerouting priority -100; policy accept;
           ip iifname "br-lan" tcp dport { 80, 8080, 7777, 6969, 2710, 1096 } \
               tproxy ip to 127.0.0.1:1088 mark set 1
       }
   }
   ```
   
   2.将本地proxy服务设置为`127.0.0.1:1088`
   
   3. IP设置如下
   
   ```bash
   # Add tproxy table
   echo "100 tproxy" >> /etc/iproute2/rt_tables
   
   # Add rule
   ip rule add fwmark 1 lookup tproxy
   
   # Add route to indicate related packets as local packets
   ip route add local 0.0.0.0/0 dev lo table tproxy
   ```
   
   
   
   

## **代理 [UA3F](https://github.com/SunBK201/UA3F)**

这一步有多个选择，可以使用本方法的`tproxy+hev-socks5-proxy`，也可以使用`ShellCrash`获知OpenClash代理，参考教程 [UA3F与Clash从0部署教程](https://sunbk201public.notion.site/UA3F-Clash-16d60a7b5f0e457a9ee97a3be7cbf557)

### **第一步：确认 `ua3f` 的监听端口**

这是最关键的前提。`hev-socks5-proxy` 需要知道应该把流量转发给谁。您需要先确定 `ua3f` 提供的本地代理（通常是 `SOCKS5` 或 HTTP）监听在哪一个端口上。

- **操作**：请您登录 `LuCI` 界面，进入 `ua3f` 的设置页面，查找类似“本地`SOCKS5`端口”或“本地HTTP端口”的设置项。
- **假设**：为了完成本教程，我们**假设 `ua3f` 的 `SOCKS5 `代理正监听在 `127.0.0.1` 的 `1080` 端口**。如果您的实际端口不同，请在后续步骤中替换成您自己的端口号。

### **第二步：配置并启动 `hev-socks5-proxy`**

现在我们来配置这个“中转站”，告诉它：从哪里接收流量，转发到哪里去。

1. **创建配置文件**：

   ```bash
   vi /etc/hevproxy.yml
   ```
   
2. 写入配置内容：

   ```yaml
   main:
     workers: 1
   
   # Set your remote socks5 proxy server like ua3f
   socks5:
     port: 1080
     address: 127.0.0.1
     # Socks5 UDP relay mode (tcp|udp)
     udp: 'udp'
     # Socks5 handshake using pipeline mode
   # pipeline: false
     # Socks5 server username
     username: 'username'
     # Socks5 server password
     password: 'password'
     # Socket mark
     mark: 0x438
   
   tcp:
     port: 1088
     address: '::'
   ```
   
   推荐配置

   ```yaml
   main:
     workers: 1
   
   socks5:
     port: 1080
     address: 127.0.0.1
     udp: 'udp'
     mark: 0x438
   
   tcp:
     port: 1088
     address: '0.0.0.0'
   
   udp:
     port: 1088
     address: '0.0.0.0'
   
   dns:
     port: 1053 
     address: '::'
     upstream: 127.0.0.1
   
   ```
   
   

   3.创建init.d里面的服务文件**`vi /etc/init.d/tproxy_service`**

   ```bash
   #!/bin/sh /etc/rc.common
   
   USE_PROCD=1
   START=95
   
   setup_tproxy() {
   
       nft delete table inet tproxy_table
       nft -f /etc/nfts/100-tproxy.nft
   
       if ! grep -q "^100.*tproxy$" /etc/iproute2/rt_tables; then
           echo "100 tproxy" >> /etc/iproute2/rt_tables
       fi
   
       if ! ip rule show | grep -q "fwmark 1 lookup tproxy"; then
           ip rule add fwmark 1 lookup tproxy
       fi
   
       if ! ip route show table tproxy | grep -q "local 0.0.0.0/0"; then
           ip route add local 0.0.0.0/0 dev lo table tproxy
       fi
   }
   
   cleanup_tproxy() {
       nft delete table inet tproxy_table 2>/dev/null
       ip route del local 0.0.0.0/0 dev lo table tproxy 2>/dev/null
       ip rule del fwmark 1 lookup tproxy 2>/dev/null
   }
   
   start_service() {
       setup_tproxy
       procd_open_instance
       procd_set_param command /usr/bin/hev-socks5-tproxy /etc/hevproxy.yml
       procd_set_param respawn
       procd_set_param stdout 1
       procd_set_param stderr 1
       procd_close_instance
   }
   
   stop_service() {
       cleanup_tproxy
       killall hev-socks5-tproxy
   }
   ```
   
3. **启动服务**：

   ```bash
   # 设置开机自启
   service tproxy_service enable
   # 立即启动服务
   service tproxy_service start
   ```

### **第三步：配置` Nftables` 防火墙规则**

最后一步，我们来配置“交通警察”，告诉它把特定的流量都送到 `hev-socks5-proxy` 那里去（也就是我们刚配置的 `1088` 端口）。

1. **查看防火墙规则**：

   ```bash
   cat /etc/nfts/100-tproxy.nft
   ```
   
3. **应用防火墙规则**：

   ```bash
   service firewall restart
   ```

**EXTRA**

使用nftables固定TTL为64

``` bash
nft add table inet ttl64
nft add chain inet ttl64 postrouting { type filter hook postrouting priority -150\; policy accept\; }
nft add rule inet ttl64 postrouting counter ip ttl set 64
```

使用iptables固定TTL为64 查看链接(https://www.zouht.com/4107.html)

``` bash
iptables -t mangle -A POSTROUTING -j TTL --ttl-set 64
```

*参考链接*
[部署到路由器认证插件](https://github.com/lurenjiamax/luci-app-cquauth)

[使用UA2F](https://blog.krytro.com/blogs/daily/240322.html)

[UA3F与Clash全教程](https://blog.sunbk201.site/posts/ua3f/)

[为什么我的路由器会被检测到]([目录](https://catalog.chn.moe/技术/OpenWrt/为啥我的路由器会被检测到/))

([haowang02/cqu-net-auth: 重庆大学校园网认证 Python 脚本](https://github.com/haowang02/cqu-net-auth))
*致谢*
@https://github.com/lurenjiamax
@https://github.com/haowang02
