# IDE
.idea/
*.iml

# Empty IntelliJ src/ dir at fork root
luci-app-cquauth/src/

# Build / staging
build_dir/
staging_dir/
tmp/
*.o
*.so

# OS noise
.DS_Store
Thumbs.db

# Windows-isms (Git Bash MSYS path conversion on /dev/null sometimes
# creates a file named "nul" in the cwd when invoking ssh through wsl)
nul
**/nul
